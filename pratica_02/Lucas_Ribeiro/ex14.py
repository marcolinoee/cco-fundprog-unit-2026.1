contador = 10
for cont in range(contador):
    print(contador)
    contador -= 1

# Ao refatorar este código utilizando o for, é perceptível que a aplicação com while é mais simples visto que definimos um limite para qualquer valor que não seja igual ou maior que 1, mas já com o a leitura é limitada ao valor pré-definido em relação de utilizanmos o while.