numeros = [
    [12, 5, 8],
    [9, 21, 3],
    [14, 6, 18]
]
maior = numeros[0][0]
menor = numeros[0][0]
for linha in numeros:
    for n in linha:

        if n > maior:
            maior = n
        elif n < menor :
            menor = n
        else:
            continue

print(maior)
print(menor)